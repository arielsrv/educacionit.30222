@model MvcSiteMapProvider.Web.Html.Models.MenuHelperModel
@using System.Web.Mvc.Html

@foreach (var node in Model.Nodes) { 
    <div class="topmenulink">@Html.DisplayFor(m => node) 
        @if (node.Children.Any()) {
           
        }
    </div>
}
